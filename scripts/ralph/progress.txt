# Ralph Progress Log
Started: 2026年 03月 05日 星期四 16:27:57 CST

## Codebase Patterns
- TuShare fetch functions: use `_request_with_retry(lambda: _query_pro(...))` pattern; filter empty string params with dict comprehension `{k: v for k, v in {...}.items() if v}`
- Sync scripts use `sync_stk_factor_pro.py` as template: parse_args, resolve_dates, load_open_dates, year_frames buffer, flush_year_buffer, mark_sync_done
- MongoDB modules: use `from app.data.mongo import get_collection`; upsert via `UpdateOne(filter, {'$set': doc}, upsert=True)` with `bulk_write`
- API routes: register in `app/api/routes/__init__.py` and `app/api/routers.py` with `dependencies=[Depends(get_current_user)]`
- No mypy/pyright installed; typecheck = Python parses and imports cleanly
---

## 2026-03-05 - US-001
- Implemented 8 TuShare fetch functions in backend/app/data/tushare_client.py
- Functions added: fetch_cyq_perf, fetch_cyq_chips, fetch_ccass_hold, fetch_hk_hold, fetch_stk_surv, fetch_moneyflow_dc, fetch_moneyflow_hsgt, fetch_idx_factor_pro
- Files changed: backend/app/data/tushare_client.py
- **Learnings for future iterations:**
  - All functions use {k: v for k, v in params.items() if v} to filter empty strings
  - fetch_idx_factor_pro was already partially in codebase; extended with full signature
  - fetch_cyq_chips uses positional ts_code (not keyword-only) per PRD spec
  - No mypy/pyright available — typecheck verified via Python import + ast.parse
---

## 2026-03-05 - US-002
- Created 4 MongoDB data layer modules: mongo_ccass_hold.py, mongo_hk_hold.py, mongo_stk_surv.py, mongo_moneyflow_hsgt.py
- Each module: create_indexes(), upsert_batch(), query functions
- Files changed: backend/app/data/mongo_ccass_hold.py, mongo_hk_hold.py, mongo_stk_surv.py, mongo_moneyflow_hsgt.py
- **Learnings for future iterations:**
  - Pattern: _INDEX_READY global flag + _get_collection() lazy init
  - upsert_batch returns result.upserted_count (not len(ops))
  - mongo_hk_hold upsert key is (ts_code, trade_date, exchange) - 3-field composite
  - mongo_stk_surv date field is surv_date (not trade_date) for indexes and queries
  - mongo_moneyflow_hsgt has unique index on trade_date
---

## 2026-03-05 - US-003
- Created backend/scripts/daily/sync_cyq_perf.py
- Pulls full-market chip performance data daily via fetch_cyq_perf(trade_date=date)
- Writes Parquet to data/features/cyq_perf/ts_code={ts_code}/year={year}/part-{uuid}.parquet
- Uses year_frames buffer pattern from sync_stk_factor_pro.py
- Fields: ts_code, trade_date, his_low, his_high, cost_5pct, cost_15pct, cost_50pct, cost_85pct, cost_95pct, weight_avg, winner_rate
- Files changed: backend/scripts/daily/sync_cyq_perf.py
- **Learnings for future iterations:**
  - sync_cyq_perf.py is a near-identical copy of sync_stk_factor_pro.py with no RENAME_MAP (cyq_perf fields don't need renaming)
  - postfix shows date/api_rows/stocks (no separate buffered count unlike stk_factor_pro)
  - No ts_code parameter passed to API — fetches entire market per trade_date
---

## 2026-03-05 - US-004
- Created backend/scripts/daily/sync_moneyflow_dc.py
- Near-identical copy of sync_cyq_perf.py with fetch_moneyflow_dc, different OUTPUT_COLUMNS, output path data/features/moneyflow_dc/
- Handles empty DataFrames gracefully (dates before 2023-09-11 have no data — expected)
- Files changed: backend/scripts/daily/sync_moneyflow_dc.py
- **Learnings for future iterations:**
  - sync_moneyflow_dc.py is a direct copy of sync_cyq_perf.py; only API function, OUTPUT_COLUMNS, output path, and sync tag differ
  - Empty DataFrame before 2023-09-11 is expected and handled by the existing no_data branch
---

## 2026-03-05 - US-005
- Created backend/scripts/daily/sync_moneyflow_hsgt.py
- Fetches HSGT aggregate money flow by date range (not per-day), up to 300 records per call (~1 year)
- Chunks large date ranges into 240-day windows for multi-year backfills
- Writes to MongoDB via mongo_moneyflow_hsgt.upsert_batch()
- Fields: trade_date, ggt_ss, ggt_sz, hgt, sgt, north_money, south_money
- Calls mark_sync_done(end_date, 'sync_moneyflow_hsgt') once at completion
- Files changed: backend/scripts/daily/sync_moneyflow_hsgt.py
- **Learnings for future iterations:**
  - moneyflow_hsgt is a date-range batch API, not per-day — no trading-day loop needed
  - Default sleep=0.5 (lighter than per-day scripts that use 2.0)
  - resolve_dates returns (start_date, end_date) tuple rather than a list of dates
  - build_chunks() splits multi-year ranges into ~240 calendar day windows
---

## 2026-03-05 - US-006
- Created backend/scripts/daily/sync_ccass_hold.py
- Per-trading-day loop: calls fetch_ccass_hold(trade_date=date), upserts via mongo_ccass_hold.upsert_batch()
- Skips non-trading days via load_open_dates (SSE trade_calendar)
- Fields: trade_date, ts_code, name, shareholding, hold_nums, hold_ratio
- Calls mark_sync_done(date, 'sync_ccass_hold') per successfully synced day
- Files changed: backend/scripts/daily/sync_ccass_hold.py
- **Learnings for future iterations:**
  - sync_ccass_hold is a near-identical copy of sync_cyq_perf but writes to MongoDB instead of Parquet — no year_frames buffer needed
  - mark_sync_done called per-date inside the loop (on success), not once at the end
  - Empty DataFrame handling is the same: log status="no_data" and continue
---

## 2026-03-05 - US-007
- Created backend/scripts/daily/sync_hk_hold.py
- Per-trading-day loop, calls fetch_hk_hold 3x per day (exchange=SH, SZ, HK), merges results, upserts via mongo_hk_hold.upsert_batch()
- Empty SH/SZ responses after 2024-08 handled gracefully (logged at DEBUG, not an error)
- Sleep is divided across 3 exchange calls (sleep/3 each) to maintain total rate limiting
- Fields: code, trade_date, ts_code, name, vol, ratio, exchange
- upsert key: (ts_code, trade_date, exchange)
- Files changed: backend/scripts/daily/sync_hk_hold.py
- **Learnings for future iterations:**
  - Per-exchange fetch failures are caught individually (warning log), not fatal — allows partial success per day
  - mark_sync_done called once per day after all 3 exchanges merged and upserted
  - Sleep distributed across 3 sub-calls to maintain API rate limits
---

## 2026-03-05 - US-008
- Created backend/scripts/daily/sync_stk_surv.py
- Paginates in 7-day sliding window chunks (stk_surv max 100 records/call)
- Calls fetch_stk_surv(start_date=chunk_start, end_date=chunk_end) per chunk
- Warns if any chunk returns exactly 100 rows (possible truncation)
- Writes to MongoDB via mongo_stk_surv.upsert_batch()
- Fields: ts_code, name, surv_date, fund_visitors, rece_place, rece_mode, rece_org, org_type, comp_rece
- Calls mark_sync_done(end_date, 'sync_stk_surv') once at end
- Files changed: backend/scripts/daily/sync_stk_surv.py
- **Learnings for future iterations:**
  - stk_surv uses date-range chunking (7-day windows) like moneyflow_hsgt, not per-day trading-day loop
  - dedup key is (ts_code, surv_date, rece_org) matching mongo_stk_surv upsert key
  - surv_date needs dash stripping (YYYY-MM-DD → YYYYMMDD) in transform_df
  - mark_sync_done called once at end (not per-chunk), same as moneyflow_hsgt pattern
---

## 2026-03-06 - US-009
- sync_cyq_chips.py was already implemented and committed (e8a39bc); PRD passes flag updated to true
- Script iterates by stock (not by date): gets ts_code list from MongoDB stock_basic via get_stock_collection()
- Supports --ts-codes for manual stock selection; year-by-year splitting when end_year - start_year > 2
- Resume support: per-year partition_exists() check skips already-synced ts_code/year combinations
- Fields: ts_code, trade_date, price, percent; dedup by (ts_code, trade_date, price)
- Files changed: backend/scripts/daily/sync_cyq_chips.py (already committed), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - sync_cyq_chips is stock-loop based (not date-loop): no open_dates/trade_calendar check needed
  - Year-split strategy: proactively splits by year for ranges > 2 years (instead of reacting to 2000-row limit)
  - Resume check happens per-year inside year-split path; non-split path re-fetches on retry
  - No mark_sync_done call (cyq_chips is historical data, not daily incremental)
  - Script does NOT check settings.tushare_token at startup (unlike other daily sync scripts)
---

## 2026-03-06 - US-010
- Created backend/scripts/daily/sync_idx_factor_pro.py
- Index-loop based (like sync_cyq_chips.py): iterates over all indices, not dates
- Index list sourced from: DEFAULT_MARKET_INDEX_CODES + market_index_dailybasic distinct ts_code + shenwan_industry distinct index_code + citic_industry distinct index_code
- Year-split strategy for ranges > 2 years; partition_exists() check for resume support
- Fields: ts_code, trade_date, open, high, low, close, pre_close, change, pct_change, vol, amount, macd_bfq, macd_dea_bfq, macd_dif_bfq, kdj_k_bfq, kdj_d_bfq, kdj_j_bfq, rsi_6_bfq, rsi_12_bfq, boll_upper_bfq, boll_mid_bfq, boll_lower_bfq, ma5_bfq, ma10_bfq, ma20_bfq, ma30_bfq, ma60_bfq, ma90_bfq, ma250_bfq
- Writes to data/features/idx_factor_pro/ts_code={ts_code}/year={year}/part-{uuid}.parquet
- No mark_sync_done call (historical data, not daily incremental)
- Files changed: backend/scripts/daily/sync_idx_factor_pro.py, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Index list build from 3 MongoDB sources with graceful fallback per source
  - SW index field is `index_code`, CITIC index field is `index_code` (same field name, different collections)
  - DEFAULT_MARKET_INDEX_CODES in mongo_market_index.py is the fallback for market indices
  - Pattern: index-loop scripts don't call mark_sync_done (only per-day scripts do)
---

## 2026-03-06 - US-011
- Created backend/app/services/market_data_service.py with 8 query functions: get_cyq_perf, get_cyq_chips, get_ccass_hold, get_hk_hold, get_stk_surv, get_moneyflow_dc, get_moneyflow_hsgt, get_index_factors
- Created backend/app/api/routes/market_data.py with 8 endpoints: GET /chip-perf/{ts_code}, /chip-distribution/{ts_code}, /ccass-hold/{ts_code}, /hk-hold/{ts_code}, /institution-survey/{ts_code}, /moneyflow-dc/{ts_code}, /moneyflow-hsgt, /index-factors/{ts_code}
- Updated backend/app/api/routes/__init__.py to export market_data_router
- Updated backend/app/api/routers.py to register market_data_router with get_current_user dependency and tag "market_data"
- Parquet queries use _query_parquet() helper via duckdb get_connection(read_only=True) + read_parquet glob pattern with union_by_name=true
- MongoDB queries delegate to existing mongo_*.py query functions
- Files changed: backend/app/services/market_data_service.py, backend/app/api/routes/market_data.py, backend/app/api/routes/__init__.py, backend/app/api/routers.py
- **Learnings for future iterations:**
  - Parquet read pattern: use read_parquet(glob, union_by_name=True) with WHERE trade_date BETWEEN ? AND ? ORDER BY trade_date DESC
  - hk-hold endpoint passes exchange as optional query param; if provided, uses query_by_exchange instead of query_by_ts_code
  - moneyflow-hsgt has no ts_code path param (aggregate data, not per-stock)
  - All Query(...) params without default make start_date/end_date required (returns 422 if missing)
---

## 2026-03-06 - US-012
- Updated backend/scripts/daily/daily.sh: TOTAL_STEPS 7 → 12
- Step 8: sync_cyq_perf.py --start-date --end-date (daily)
- Step 9: sync_moneyflow_dc.py --start-date --end-date (daily)
- Step 10: sync_moneyflow_hsgt.py --start-date --end-date (daily)
- Step 11: sync_ccass_hold.py + sync_hk_hold.py (Friday only, reuse TARGET_WEEKDAY)
- Step 12: sync_stk_surv.py --last-days 7 (Friday only)
- sync_cyq_chips.py and sync_idx_factor_pro.py NOT added (historical-only, too slow for daily)
- Files changed: backend/scripts/daily/daily.sh, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Steps 11/12 reuse the existing TARGET_WEEKDAY variable set in Step 4 logic
  - Step 11 chains two Python scripts with && so both must succeed
  - sync_stk_surv uses --last-days 7 (not --start-date/--end-date) for weekly runs
---
